# Debug
# Set DEBUGCLI=YES in .zshenv or un comment:
#export DEBUGCLI=YES
if [[ $DEBUGCLI == "YES" ]]; then
  echo "Start .zprofile" # Start
  debug_file_call
fi


# Run .zshrc file is it exists
## Customizing Your Terminal: .bash_profile and .bashrc files - https://www.youtube.com/watch?v=vDOVEDl2z84
# if [ -f ~/.zshrc ]; then
#     source ~/.zshrc
# fi


# The End
if [[ $DEBUGCLI == "YES" ]]; then
  echo "End .zprofile"
fi
