[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[user]
	name = Tomas Bäckman
	email = {{ .email | quote }}
[credential]
	helper = osxkeychain
